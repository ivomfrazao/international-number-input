import { CountryStandardNumberUtils } from '../../../types';
import nifp from './nifp';

const countryNumberUtils: CountryStandardNumberUtils = {
	iso2: "GN",
	numberUtils: [
		nifp
	]
}

export default countryNumberUtils;
